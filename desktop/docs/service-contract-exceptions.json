{
    "$comment": "Intentionally unintegrated JSON-RPC methods for desktop ↔ services contract checks. Each ignoredMethods entry needs a current technical reason. Edit by hand; run npm run service-contracts:write to refresh docs/services-api.md after services/ changes.",
    "ignoredMethods": {
        "engine:settings-projection": "Broker-to-engine-manager full settings projection for the pinned peer subscription hub. Consumed by Manager.handleSettingsMessage; the desktop consumes engine:settings-changed instead.",
        "engine:settings-reply": "Correlated broker-to-engine-manager reply to the pinned peer settings relay and the configure operation's narrow proxy rebind callback. Consumed by settingsRelay.reply, never a renderer event.",
        "engine:settings-request": "Engine-manager-to-broker correlated request for settings get/preview/apply or a validated operation's proxy rebind. Consumed by Broker.handleSettingsRelay.",
        "engine:settings-cancel": "Engine-manager-to-broker cancellation for a disconnected or revoked peer request. Consumed by Broker.forwardEngineNotification; accepted operations complete under the target's independent deadline.",
        "schedule:priority": "Broker-internal. nvpair-job-scheduler emits a per-engine priority snapshot ({engine, nodes, ranks?} — ordered node ids plus each node's pending count) to nvpair-ui-broker, which forwards it to nvpair-proxy via node/set-priority; the process then layers its shared optimistic burst reservations on that baseline. PAIR consumes the routing result rather than this notification, and must not mirror the pending/reservation accounting.",
        "engine:restore-enabled": "Broker-internal startup restoration. nvpair-ui-broker emits engine:restore-enabled directly to its supervised engine-manager after the managed Ollama port gate and on manager respawn; it is not a renderer/UI notification.",
        "ollama-proxy:ready": "Consumed, not missing: the broker relays it and normalizeBrokerProxy (modular-supervisor.ts) strips the `ollama-proxy:` prefix, so the bridge handles the de-prefixed `ready` (sets proxyPort). The literal `ollama-proxy:ready` is intentionally absent from our TS — extractor limitation, not a gap.",
        "lmstudio-proxy:ready": "Consumed, not missing: the LM Studio counterpart of ollama-proxy:ready, de-prefixed by the same normalizeBrokerProxy loop. It only became visible to the checker when METHOD_RE started accepting hyphens in a namespace segment; before that the whole lmstudio-proxy:* surface was silently unmatched.",
        "node/selection-changed": "Automatic routing has no selected-node UI, so PAIR deliberately does not consume proxy selection changes.",
        "proxy/request": "Per-request proxy telemetry is not rendered; workload lifecycle uses the broker workloads stream.",
        "proxy/request-started": "Per-request proxy telemetry is not rendered; see proxy/request.",
        "workload:submitted": "Producer-to-broker lifecycle event. PAIR consumes the broker's translated workloads:upsert stream rather than raw workload events.",
        "workload:started": "Proxy-to-broker lifecycle event translated by the broker into workloads:upsert, which PAIR consumes.",
        "workload:completed": "Proxy-to-broker terminal lifecycle event translated by the broker into workloads:upsert.",
        "workload:errored": "Proxy-to-broker terminal lifecycle event translated by the broker into workloads:upsert.",
        "node/activity": "Proxy-to-broker liveness evidence, consumed entirely inside the backend. Each nvpair-proxy instance raises it (coalesced to one report per node per 2s by nvpair-shared/nodeactivity) whenever a peer's engine returns inference response bytes; nvpair-ui-broker relays it to nvpair-node-scanner as discovery:node-activity, where it keeps a node that is busy serving inference from being evicted for failing a liveness probe it had no spare CPU to answer. PAIR consumes the result — the node staying in the discovery snapshot — not this per-request notification, which carries no state a UI could render. Not currently surfaced by the extractor either (it is emitted through a shared noderec constant, like nodeinfo:observed-addresses); this entry records the intent regardless.",
        "errors:report": "Producer-to-broker event demultiplexed into broker-owned nvpair-errors. PAIR consumes the resulting errors:update snapshot.",
        "errors:clear": "Producer-to-broker event demultiplexed into broker-owned nvpair-errors. PAIR consumes the resulting errors:update snapshot.",
        "cluster:trust-changed": "Broker-internal. nvpair-cluster-manager announces it from its trust store after a pin write lands, and nvpair-ui-broker answers it by calling discovery:reload-trust on nvpair-node-scanner so the scanner re-derives each peer's trusted annotation. PAIR consumes the result — the refreshed AvailableNode.trusted carried on the discovery snapshot — not this notification. It carries no payload by design: recipients re-read the cluster dir rather than trusting a diff on the wire.",
        "connection/cluster-auto-sync": "The setting has no active backend behavior and no PAIR control, so its notification is intentionally not consumed.",
        "cluster:invite-declined": "Inviter-side-only outbound-invite terminal signal (unlike cluster:invite-expired, which the backend also emits on the receiver and PAIR consumes). PAIR's outbound pairing UI (useInvitePairing) learns the declined state by polling cluster:invite-status, and any solo cluster PAIR auto-created only to back the invite is dissolved via cluster:abandon-if-solo (the backend's own provenance-safe auto-leave also surfaces as the cluster:identity-changed + nodes:changed pushes PAIR already consumes). The receiver-side counterpart, cluster:invite-canceled, IS consumed. This push is a latency optimization PAIR does not require."
    }
}
